// axe.config.ts
export const axeConfig = {
    tags: ['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa'], // WCAG compliance tags
      exclude: ['#known-issue-element'], // Common exclusions, if any
  };
  